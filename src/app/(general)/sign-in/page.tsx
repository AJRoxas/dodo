import { SignInSection } from '@/components/sign-in-sign-up/Authentication'

const SignInPage = () => {
  return(
    <main>
      <SignInSection></SignInSection>
    </main>
  );
};

export default SignInPage;